import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourjourney.controlroom',
  appName: 'Control Room: Admin Dashboard',
  webDir: 'dist'
};

export default config;
